import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'calculadora_aula',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
